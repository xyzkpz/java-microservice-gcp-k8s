server.port=8080

# Application properties (will be overridden by ConfigMap)
app.name=Java Microservice
app.version=1.0.0
app.environment=development
app.message=Welcome to the Java Microservice on GCP!

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
