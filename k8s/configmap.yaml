apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  # Application configuration
  app.name: "Java Microservice on GCP"
  app.version: "1.0.0"
  app.environment: "production"
  app.message: "Hello from GCP Kubernetes Cluster!"
  
  # You can add more configuration here
  # database.url: "jdbc:postgresql://db-service:5432/mydb"
  # cache.ttl: "3600"
